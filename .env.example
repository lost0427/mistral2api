# 复制为 .env 后修改，与 keys.txt 一起挂载到容器
# docker run -v ./.env:/app/.env:ro -v ./keys.txt:/app/keys.txt:ro -p 8082:8082 mistral2api

# 网关鉴权（客户端 Bearer Token，逗号分隔，留空不鉴权）
GATEWAY_KEYS=sk-your-gateway-key

# 上游代理（可选）
# PROXY=http://host:7890

# 监听（可选）
# PORT=8082
# HOST=0.0.0.0
# COOLDOWN=60
